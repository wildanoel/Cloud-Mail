# Example frontend env. Copy to .env.local if you need overrides.
# No secrets belong here - only public Vite config.

NODE_ENV=dev
VITE_APP_TITLE=Cloud Mail
VITE_BASE_URL=http://127.0.0.1:8787/api
VITE_PWA_NAME=Cloud Mail
